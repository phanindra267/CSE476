# Flight Option Finder Agent — Environment Configuration
# No API keys required — this project uses deterministic local data.

MODEL_PROVIDER=local
MODEL_NAME=deterministic
# API_KEY=  # Not required for local deterministic mode
